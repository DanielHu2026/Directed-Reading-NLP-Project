## Config notes

- `configs/config.yaml` is the single source of truth for:
  - where raw data lives
  - where derived data should be written
  - how to define "high-gap" words
  - basic text preprocessing switches
